import { css } from '@emotion/react';
import { themeType as t, instanceType as o } from '@ps/ui/styles/theme';

export default {
  default: (theme: t) => css`
    display: block;
    position: relative;
  `,
};
