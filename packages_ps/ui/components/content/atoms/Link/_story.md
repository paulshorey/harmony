This renders an `<a>` anchor tag. Intelligently parses props and corrects or fills in missing information to ensure the rendered anchor tag is valid, with good accessibility, and using canonical URL.
