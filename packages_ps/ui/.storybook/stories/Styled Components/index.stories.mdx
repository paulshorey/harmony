import { Meta, Story, Canvas } from '@storybook/addon-docs';
import Box from '@ps/ui/components/content/Box';
import AppProvider from '@ps/ui/components/utils/AppProvider';
import {
  Wrapper,
  Container,
  Title,
  Content,
  Button,
} from '@ps/ui/.storybook/components/Components';

<Meta title="Styled Components" id="about-3" />

<AppProvider>
<Wrapper>

<code>
  {`
import Box, { withBox } from "@ps/ui/components/content/Box";
import { withButton } from "@ps/ui/components/form/Button";
\n
export const Container = withBox({
  ss: (props) => \`padding: 1.2rem 1rem 1.9rem; \${props.theme.instance.variants["bgGradient"] && \`padding-top: 1.3rem;\`}\`,
});
export const Content = withBox({ ss: \`padding: 0\` });
export const Title = withBox({
  variant: "textGradient",
  ss: \`padding: 0.125rem 0 0 0.25rem;\`,
});
export const Button = withButton({
  ss: \`margin: 1rem 1rem 0rem 0.25rem;\`,
});
`}
</code>

<Container variant="bgLight">
  <Title color="cta1">on light:</Title>
  <Content>
    <Button variant="bgGradient" color="cta1">
      gradient cta1
    </Button>
    <Button color="cta1">default cta1</Button>
    <Button>default</Button>
    <Button variant="link" color="cta1">
      link
    </Button>
    <Button variant="disabled" color="cta1">
      disabled
    </Button>
  </Content>
</Container>

<Container variant="bgGradient" color="cta1">
  <Title color="cta2">on gradient:</Title>
  <Content>
    <Button variant="bgGradient" color="cta2">
      gradient cta2
    </Button>
    <Button color="cta2">default cta2</Button>
    <Button>default</Button>
    <Button variant="link" color="cta2">
      link
    </Button>
    <Button variant="disabled" color="cta2">
      disabled
    </Button>
  </Content>
</Container>

<Container variant="bgDark" onDark>
  <Title onDark>on dark:</Title>
  <Content>
    <Button variant="bgGradient" color="cta1">
      gradient cta1
    </Button>
    <Button color="cta1">default cta1</Button>
    <Button>default</Button>
    <Button variant="link" color="cta1">
      link
    </Button>
    <Button variant="disabled" color="cta1">
      disabled
    </Button>
  </Content>
</Container>

</Wrapper>
</AppProvider>
