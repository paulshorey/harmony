import { Meta, Story, Canvas } from '@storybook/addon-docs';
import Box from '@ps/ui/components/content/Box';
import Code from '@ps/ui/components/content/Code';
import CodeInline from '@ps/ui/components/content/CodeInline';
import AppProvider from '@ps/ui/components/utils/AppProvider';
import Wrapper from '@ps/ui/.storybook/components/Wrapper';
import { SvgSparkle } from '@ps/ui/.storybook/components/Decorations';
import MediaQueriesInline from '@ps/ui/.storybook/components/MediaQueriesInline';

<Meta title="Why another UI library ?" id="about-1" />

<AppProvider>

<Wrapper ssAll="a { color: white; }">

<h1>Harmony UI</h1>

<p>
  This library is made for <strong>both</strong> software engineering and web
  development, for more <strong>intuitive and maintainable</strong> web
  products.
  <br />
  Instead of trying to rewrite CSS in JS, this allows{' '}
  <strong>CSS and JS</strong> to coexist in harmony, each doing what it does
  best.
  <br />
  Still needs many more components and unit tests, but if you like this
  approach, <a>please contribute or comment!</a>
</p>

<br />

<h2>
  This is not just a Component Library...{' '}
  <Box as="span" ss="float:right;padding: 0 0.33rem 0 0; display:none">
    Github link goes here
  </Box>
</h2>

It's a system for easily <Box as="b" variant="textGradient" data-color="accent">bootstrapping and maintaining</Box> your own <Box as="b" variant="textGradientUnderlined" data-color="rainbow"> custom branded component library</Box> as part of a <Box as="b" variant="textGradient" data-color="accent"><SvgSparkle ss="" />multi-repo<SvgSparkle ss="" /><SvgSparkle ss="" /></Box>. <span className="noWrap">This includes an innovative responsive styling system</span> that makes implementing initial designs and redesigns faster and easier.

The styled text above is the most basic example of variants. Variants are like classNames but in Typescript, without name collisions.

But it's not just for styling &ndash; use variants to _add new JS functionality_ without _breaking or complicating_ existing implementations.

<Code
  variant="transparentDark"
  code={`import Box from 'harmonyui/components/content/Box';
...
<Box as="b" variant="textGradient" data-color="accent"> bootstrapping and maintaining </Box>
<Box as="b" variant="textGradientUnderlined" data-color="rainbow"> custom branded component library</Box>`}
/>

StyledComponents combined with a shortcut `ss` prop (like Emotion's `css` or Mantine's `sx`) make it easy to add unique styles to markup.

<Code
  variant="transparentDark"
  code={`import { SvgSparkle1, SvgSparkle2, SvgSparkle3 } from 'harmonyui/.storybook/components/Decorations';
...
<SvgSparkle1 ss="top:-1rem;" /> multi-repo <SvgSparkle2 ss="bottom:-1rem;right:-3rem;" />`}
/>

<p>
  Media query shortcuts let you painlessly style for multiple devices! You're
  not limited to just{' '}
  <CodeInline variant="text" data-color="dark">
    xs,sm,md,lg
  </CodeInline>{' '}
  or whatever you originally defined. Not limited to{' '}
  <CodeInline variant="text" code={`min-width`} />. Use{' '}
  <CodeInline variant="text" code={`props.ssTablet`} /> (or{' '}
  <CodeInline variant="text" code={`props.theme.mq.tablet`} /> in your style
  function) to style tablets (between 501-1024px). Each media query prop can be
  a <CodeInline variant="text" code={`string`} />,{' '}
  <CodeInline variant="text" code={`(props)=>string`} /> function, or CSS-in-JS
  object.
</p>

<Code
  variant="transparentDark smaller"
  code={`import { withBox } from 'harmonyui/components/content/Box';
const Demo = withBox({ ss: 'display:none;' });
...
<Demo ssWebview="display:block"> apply style only if rendered inside an in-app browser (like Gmail or Facebook) </Demo>
<Demo ssIframe="display:block"> apply style only if rendered inside an iframe (like Storybook or website widget) </Demo>
// You can adjust breakpoint sizes in your theme.
<Demo ssPhone="display:block"> style only phones (<=500px) </Demo>
<Demo ssSmallPhone="display:block"> style only small phones like the Mini or X (<=400) </Demo>
<Demo ssLargeTablet="display:block"> style only larger tablets (768-1024px) </Demo>
// People's desktop screens are very rarely <=1024px nowadays. Most iPads are 1024px wide. 
<Demo ssDesktop="display:block"> only Desktop ( >1024px )</Demo>
<Demo ssMobile="display:block"> only Mobile ( <=1024px )</Demo>
<Demo ssLg="display:block"> "large" screens including larger tablets ( >930px )</Demo>
<Demo ssSm="display:block"> "small" screens including smaller tablets ( <=930px )</Demo>
// And many more. Each is only included in the bundle if you use it. `}
/>

<br />

### Maintainable

- Great developer experience. Easily manage media queries, variables, colors, markup, and interactive functionality. Tree shaking by default.

### Mono-repo

- Maintain just one set of components and styles. Import into multiple apps. If one app needs unique style or functionality, it's easy to override both.

### Extensible

- Add functionality and style to the base library as a "variant". Then select that variant in your app. This way if you need to customize a component, you don't have to duplicate code. Extend a component for one app without affecting other apps.

<br />

## CSS-in-JS is no longer tedious! Read more:

- <a href="/" data-color="accent">
    Media queries
  </a> - you can style for multiple devices with JSX props, JS variables, or Styled
  Components
- <a href="/">Variants and variables</a> - choose from any variant, add a new one,
  use color/scheme/props in your styles

</Wrapper>

</AppProvider>
