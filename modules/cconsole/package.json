{
  "name": "@ps/cconsole",
  "version": "0.0.8",
  "description": "No dependencies. No setup required. Easily integrate with Cloud logging (LogDNA, etc). Visually group similar types of logs in the terminal. Show a concise file/line trace for each message - so you can actually find where it's originating without looking through the entire codebase.",
  "private": true,
  "author": "Paul Shorey <paulshorey.com>",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^29.1.2",
    "jest": "^29.1.2",
    "ts-jest": "^29.0.3",
    "ts-mocha": "^10.0.0",
    "ts-node": "^10.9.1",
    "ts-patch": "^2.0.2",
    "typescript": "^4.8.4"
  },
  "scripts": {
    "node:dev": "bash _bash/_start_tsc_dev.sh && echo 'develop using Typescript (watch)'",
    "archived-umd": "rm -rf .parcel-cache && rm -rf umd && mkdir umd && rm -rf umd && parcel build esm/*.js -d umd --public-url .",
    "build": "tsc --downlevelIteration",
    "dev": "tsc --watch",
    "mocha": "ts-mocha test/**/*.spec.ts -w --watch-files '**/*.ts'",
    "jest": "jest"
  },
  "husky": {
    "hooks": {
      "pre-commit": "npm version patch --no-git-tag-version && npm run test && git add ."
    }
  },
  "targets": {
    "app": {
      "engines": {
        "browsers": "> 0.75%"
      }
    }
  },
  "type": "module",
  "exports": {
    ".": "./dist/index.js",
    "./cconsoleInit": "./dist/cconsoleInit.js",
    "./cconsole": "./dist/cconsole.js",
    "./src": "./src/index.js",
    "./src/cconsoleInit": "./src/cconsoleInit.js",
    "./src/cconsole": "./src/cconsole.js",
    "./package.json": "./package.json"
  }
}
