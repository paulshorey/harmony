import { Meta, Story, Canvas } from '@storybook/addon-docs';
import Block from '@ps/ui/components/content/Block';
import Inline from '@ps/ui/components/content/Inline';
import Code from '@ps/ui/components/content/Code';
import CodeInline from '@ps/ui/components/content/CodeInline';
import ThemeProvider from '@ps/ui/components/utils/ThemeProvider';
import Wrapper from '@ps/ui/.storybook/components/Wrapper';
import {
  BulletNumber,
  SvgSparkle,
} from '@ps/ui/.storybook/components/Decorations';

<Meta title="Why another UI library ?" />

<ThemeProvider>

<Wrapper ssAll="a { color: white; }">

<Block as="h1" textgradient="rainbow">
This is a development kit
</Block>

for bootstrapping and maintaining a <Inline as="b" textgradient="rainbow"> custom branded component library</Inline>. 

<Block ss="margin-bottom:0.75rem;line-height:1.33rem;">
<Inline as="h2" textgradient="accent">1.</Inline> <Inline textcolor="accent" ss="font-size:1.125rem;">pre-configured mono repo apps and modules</Inline> <Inline ss="font-size:1rem;font-weight:200;"> - even with TurboRepo, getting everything to work together is hard!</Inline>
</Block>

<Block ss="margin-bottom:0.75rem;line-height:1.33rem;">
<Inline as="h2" textgradient="accent">2.</Inline> <Inline textcolor="accent" ss="font-size:1.125rem;">a styling system to make branding easier</Inline> - especially for different screen sizes! Both prototyping and maintenance.
</Block>

<Block ss="margin-bottom:0.75rem;line-height:1.33rem;">
<Inline as="h2" textgradient="accent">3.</Inline> <Inline textcolor="accent" ss="font-size:1.125rem;">and a few components to get started</Inline> - this isn't a finished library but a system for maintaining custom components.
</Block>

<br />

---

<Block as="h2" ss="margin-top:2rem;" textgradient="rainbow">
For better DX and UX
</Block>

<p>
  <a>Media query shortcuts</a> let you{' '}
  <Inline as="b" textgradient="rainbow">
    painlessly style for multiple devices!
  </Inline>{' '}
  You're not limited to just{' '}
  <CodeInline variant="text" color="purple">
    xs,sm,md,lg
  </CodeInline>{' '}
  or whatever you originally defined. Not limited to{' '}
  <CodeInline variant="text" code={`min-width`} /> either. Instead, use semantic
  prop names like <CodeInline variant="text" code={`ssTablet`} /> (or{' '}
  <CodeInline variant="text" code={`theme.mq.tablet`} />) to style tablets
  (between 601-1024px). Each media query prop can accept a{' '}
  <CodeInline variant="text" code={`string`} />,{' '}
  <CodeInline variant="text" code={`(props)=>string`} /> function, or any
  EmotionCSS type.
</p>

<Code
  variant="transparentDark"
  code={`import { withBlock } from '@ps/ui/components/content/Block';
const Demo = withBlock({ ss: 'display:none;' });
// For convenience, ss props accept a simple string in addition to any EmotionCSS type. No configuration is required.
<Demo ssWebview="display:block"> apply style only if rendered inside an in-app browser (like Gmail or Facebook) </Demo>
<Demo ssIframe="display:block"> apply style only if rendered inside an iframe (like Storybook or website widget) </Demo>
// You can adjust breakpoint sizes in your theme.
<Demo ssPhone="display:block"> style only phones (<=600px) </Demo>
<Demo ssSmallPhone="display:block"> style only small phones like the Mini or X (<=400) </Demo>
<Demo ssLargeTablet="display:block"> style only larger tablets (768-1024px) </Demo>
// People's desktop screens are very rarely <=1024px nowadays. Most iPads are 1024px wide. 
<Demo ssDesktop="display:block"> only Desktop ( >1024px )</Demo>
<Demo ssMobile="display:block"> only Mobile ( <=1024px )</Demo>
<Demo ssLg="display:block"> "large" screens including larger tablets ( >930px )</Demo>
<Demo ssSm="display:block"> "small" screens including smaller tablets ( <=930px )</Demo>
// And many more. Each breakpoint is only included in the bundle if you use it. `}
/>

**Pre-define any props, not only styles.** Then add more specific props or styles per instance:  

<Code
  variant="transparentDark"
  code={`// 1. Use the provided named export to pre-define props:
import { withButton } from '@ps/ui/components/focus/Button';
const Button = withButton({
  ...props,
  size: 'sm',
  icon: <FontAwesomeIcon icon={faUser} />,
  ss: 'margin: 0 0.875rem 0.875rem 0;',
  onClick: () => alert('clicked!'),
});
// 2. Use your component. Pass unique props per instance:
<Button loading>
  Loading
</Button>
<Button variant="outlined">
  Outlined
</Button>
<Button variant="text">
  Text
</Button>
<Button round />`} />


**You don't have to use style strings.** That's just for convenience. Use any type supported by <CodeInline variant="text" code={`@emotion/styled`} />.

<Code
  variant="transparentDark"
  code={`<Block ss={(props) => css\` padding: 1rem \${props.theme.sizes.card.paddingX}; \`}>`} />

<!--Or use your own preferred solution such as <CodeInline variant="text" code={`props.style`} />, <CodeInline variant="text" code={`props.className`} />, <CodeInline variant="text" code={`props.css`} /> or <CodeInline variant="text" code={`props.sx`} />. -->
  
This library does not use Emotion's preferred <CodeInline variant="text" code={`props.css`} /> because it's not portable, it needs to be set up by every app. But since the css prop actually gets compiled to a standard className, you can use it in any app. Instead, this library adopted the syntax <CodeInline variant="text" code={`props.ss`} />.

<a href="http://localhost:6006/?path=/docs/core-styled-components--page" target="_top">Read more</a> about those <CodeInline variant="text" code={`ss`} /> props, the way styled-components are implemented in this library, and more advanced tricks for coloring elements' text and background.

<br />
<br />

<Block as="h2" ss="margin-top:2rem;">
  <Block as="span" textgradient="rainbow">
    CSS-in-JS is no longer tedious!
  </Block>
</Block>

<p>
  <Inline ssPhone="display:none;">👈 </Inline>
  <Inline ssNotPhone="display:none;">👇 </Inline>
  <Inline textcolor="accent">&nbsp;Go ahead and explore the sidebar.&nbsp;</Inline>
  Below each component's story is a documentation of all the props it accepts.
</p>

<p>
  <br />
  This is my latest experiment. Not production ready yet. Needs much more testing. But it's already useful.
  <br />
  If you like this approach then <a>checkout the Github repo</a>. Please contribute
  or open an issue to discuss. Thank you!
  <br />
  Please reach out with any questions or ideas. Let's collaborate! ~ <a
    href="https://paulshorey.com"
    target="_blank"
  >
    Paul Shorey
  </a>
</p>

</Wrapper>

</ThemeProvider>
