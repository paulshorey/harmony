import { Meta, Story, Canvas } from '@storybook/addon-docs';
import Block from '@ps/ui/components/content/Block';
import Code from '@ps/ui/components/content/Code';
import Inline from '@ps/ui/components/content/Inline';
import Button from '@ps/ui/components/focus/Button';
import ThemeProvider from '@ps/ui/components/utils/ThemeProvider';
import { Buttons } from './_components';

<Meta title="core/Styling and coloring" />

<ThemeProvider>

<Block ss={`
  position:absolute;
  top:0;
  left:0;
  width:100%;`}>

<Block
  textcolor="light"
  variant="articleX"
  ss="padding-top:1.25rem;padding-bottom:0.95rem;"
>
  <p>
    All components in this group have "onFocus / onBlur" states. Accessible
    using keyboard Arrows/Enter/Esc/Tab.
  </p>
  <p>
    Forms, dashboard elements, links, disclosures, modals, and any kind of
    editable content that can be interacted with using a keyboard.
  </p>
</Block>

<Block textcolor="light" bgcolor="purple" ss="padding: 1rem;">
  <Block variant="articleX" ss="padding-top:1.25rem;">
    <Buttons />
    <Button size="sm" textcolor="rainbow" ss="margin: 0 0.875rem 0.875rem 0;">
      Rainbow
    </Button>
    <Button
      variant="outlined"
      size="sm"
      textgradient="rainbow"
      ss="margin: 0 0.875rem 0.875rem 0;"
    >
      Rainbow outline
    </Button>
    <Code
      collapsed={true}
      code={`// import the ready-to-use default export (Button), or the HOC named export (withButton)
import { withButton } from '@ps/ui/components/focus/Button';
const Button = withButton({
  size: 'sm',
  ss: 'margin: 0 0.875rem 0.875rem 0;',
});
  
<Block textcolor="light" bgcolor="purple">
   
  <Button loading={true}>Loading</Button>
  <Button variant="outlined">Outlined</Button>
  <Button variant="text" icon={<FontAwesomeIcon icon={faUser} />}>
    Text
  </Button>
  <Button round={true} icon={<FontAwesomeIcon icon={faUser} />}>
    Round
  </Button>
  <Button
    round={true}
    variant="primary"
    icon={<FontAwesomeIcon icon={faUser} />}
  />
  
  <Button textcolor="rainbow">
    Rainbow
  </Button>
  <Button variant="outlined" textgradient="rainbow">
    Rainbow outline
  </Button>
  
</Block>`}
    />
  </Block>
</Block>

<Block textcolor="purple" bgcolor="light" ss="padding: 1rem;">
  <Block variant="articleX">
    <Block as="h3">Different color scheme:</Block>
    <Buttons />
    <Button size="sm" textcolor="cta" ss="margin: 0 0.875rem 0.875rem 0;">
      Different color
    </Button>
    <Button
      variant="outlined"
      size="sm"
      textgradient="cta"
      ss="margin: 0 0.875rem 0.875rem 0;"
    >
      With outline
    </Button>
    <Code
      collapsed={true}
      code={`// Exactly same code for buttons. Just change the wrapper colors.
<Block textcolor="purple" bgcolor="light">
  ...
  <Button textcolor="cta">Different color</Button>
  <Button fggradient="cta" variant="outlined">With outline</Button>
</Block>`}
    />
  </Block>
</Block>

<Block
  textcolor="light"
  variant="articleX"
  ss="padding-top:1.25rem;padding-bottom:0.95rem;"
>
  <p>More coming soon!</p>
</Block>

</Block>
</ThemeProvider>
