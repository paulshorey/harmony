import { css } from '@emotion/react';

export default {
  default: (props) => css`
    fieldset {
      border-color: var(--color-text) !important;
    }
    * {
      color: var(--color-text) !important;
    }
  `,
};
