A more complex implementation of the Select component. This adds a text input and + button to the bottom of options, so user can type a custom value. 

All the same props as Select (except takes values instead of options). For example, pass `suffixIcon={<FontAwesomeIcon icon={faUser} />}` to add an icon to the right of the input. TODO: need to add validation, to keep the values clean.
