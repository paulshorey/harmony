import React from 'react';
import Fieldset from '.';

export default {
  title: 'Design Systems/Atoms/InputFieldset',
  component: Fieldset,
};

const Template = (args) => <Fieldset {...args} />;

export const Default = Template.bind({});
Default.args = {};
